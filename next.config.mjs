/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com','via.placeholder.com','rukminim2.flixcart.com','images.unsplash.com'],
    },
  };

export default nextConfig;
